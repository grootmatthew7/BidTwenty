package com.bidtwenty.sports;

/**
 * Canonical identifiers for every sport adapter the application can host.
 * Currently the default implementation is NBA; other sports can be added here
 * without changing the room lifecycle.
 */
public enum SportId {
    NBA
}
